<html>
<head>
  <title>nuntium<%= ' - ' + @application.name if !@application.nil? && !@application.id.nil? %></title>
  <link rel="stylesheet" href="/stylesheets/scaffold.css" type="text/css"/>
  <script src="/javascripts/jquery-1.3.2.js" type="text/javascript"></script>
  <script src="/javascripts/application.js" type="text/javascript"></script>
  <%= yield :head%>
</head>
<body>
<% if @hide_title.nil? %>
  <% if !@application.nil? && !@application.id.nil? %>
    <h2 style="color:blue;float:left">nuntium</h2>
    <h3 style="text-align:right"><%= @application.name %> | <%= link_to 'Help', 'http://code.google.com/p/nuntium/', :target => '_blank' %> | <%= link_to 'Logoff', :controller => :home, :action => :logoff %></h3>
  <% else %>
    <h2 style="color:blue">nuntium</h2>
  <% end %>
<% end %>
<%= yield %>
</body>
</html>
